name: Run stock screener

on:
  schedule:
    # GitHub Actions cron is UTC-only, so 9:00 AM US/Eastern needs two
    # triggers to stay correct across DST -- the check-time job below picks
    # whichever one actually lands at 9 AM Eastern and skips the other.
    - cron: "0 13 * * 0"  # ~9:00 AM Eastern during EDT (UTC-4)
    - cron: "0 14 * * 0"  # ~9:00 AM Eastern during EST (UTC-5)
  workflow_dispatch: {}  # allows manual trigger from the Actions tab

jobs:
  check-time:
    runs-on: ubuntu-latest
    outputs:
      proceed: ${{ steps.check.outputs.proceed }}
    steps:
      - id: check
        run: |
          if [ "${{ github.event_name }}" != "schedule" ]; then
            echo "proceed=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          HOUR=$(TZ="America/New_York" date +%H)
          if [ "$HOUR" = "09" ]; then
            echo "proceed=true" >> "$GITHUB_OUTPUT"
          else
            echo "proceed=false" >> "$GITHUB_OUTPUT"
          fi

  screen:
    needs: check-time
    if: needs.check-time.outputs.proceed == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run screener
        id: run_screener
        env:
          SEC_USER_AGENT: ${{ secrets.SEC_USER_AGENT }}
        run: python screener.py

      # continue-on-error so a transient Google API hiccup (e.g. a 503)
      # doesn't cost the whole week's report -- update_history.py already
      # retries transient errors itself; this is the last-resort fallback.
      # The step still shows as failed in the Actions UI so it's visible.
      - name: Update history sheet and compute new names
        id: update_history
        continue-on-error: true
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
        run: python update_history.py

      - name: Fall back if history update failed
        if: steps.run_screener.outcome == 'success' && steps.update_history.outcome != 'success'
        run: |
          for f in new_this_week.csv new_this_week_distress.csv; do
            printf 'symbol,note\n,"History sheet update failed this run -- new-name diff unavailable this week. See the workflow logs. Full results are attached separately."\n' > "$f"
          done

      # Committed (not just uploaded as an artifact) so the weekly one-pager
      # research routine -- a separate Claude Code cloud agent that can't
      # access this repo's Actions artifacts or local files -- can read it
      # via a plain git clone.
      - name: Commit weekly candidate lists for the research routine
        if: steps.run_screener.outcome == 'success'
        run: |
          mkdir -p weekly-lists
          cp new_this_week.csv weekly-lists/candidates.csv
          cp new_this_week_distress.csv weekly-lists/candidates_distress.csv
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add weekly-lists/candidates.csv weekly-lists/candidates_distress.csv
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update weekly candidate lists ($(date -u +%Y-%m-%d))"
            git push origin HEAD:main
          fi

      - name: Upload results
        if: steps.run_screener.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: screener-results
          path: |
            screener_results.csv
            distress_results.csv
            new_this_week.csv
            new_this_week_distress.csv

      - name: Email results
        if: steps.run_screener.outcome == 'success'
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.GMAIL_USERNAME }}
          password: ${{ secrets.GMAIL_APP_PASSWORD }}
          subject: "Stock Screener - ${{ steps.update_history.outcome == 'success' && 'New Names' || 'Results (history update failed)' }} - ${{ github.event.repository.name }}"
          to: warrenempey@gmail.com
          from: Stock Screener <${{ secrets.GMAIL_USERNAME }}>
          body: "Attached are this week's newly qualifying companies from both screens: the up-screen (appreciated >35%, opportunistic raise candidates) and the distress-screen (declined >25%, capital-need candidates), plus full results for each for reference."
          attachments: new_this_week.csv,new_this_week_distress.csv,screener_results.csv,distress_results.csv
